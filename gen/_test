import * as React from 'react';
import mount from '../../../utils/mount';
import %COMPONENT% from './';

test('%COMPONENT% should have at least one functional test', () => {
  mount(<%COMPONENT%>test</%COMPONENT%>);

  expect(true).toEqual(false);
});